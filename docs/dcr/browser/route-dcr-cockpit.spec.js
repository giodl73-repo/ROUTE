const { test, expect } = require("@playwright/test");
const path = require("path");

const pagePath = "file:///" + path.resolve(__dirname, "route-dcr-cockpit.html").replace(/\\/g, "/");

test.describe("ROUTE DCR cockpit", () => {
  test("desktop exposes the simulated operating board and claim boundaries", async ({ page }) => {
    await page.setViewportSize({ width: 1440, height: 900 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await expect(page.getByRole("heading", { name: "ROUTE DCR Cockpit", exact: true })).toBeVisible();
    await expect(page.getByLabel("Simulated service network")).toBeVisible();
    await expect(page.getByText("Mode advisory simulation")).toBeVisible();
    await expect(page.getByText("traffic control, legal detour, SLA, EV availability")).toBeVisible();
    await expect(page.locator("#active-case")).toHaveText("Winter closure and EV stress");
    await expect(page.locator("#recommended-posture")).toHaveText("weather-responsive split + EV staging; verify 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#alert-title")).toHaveText("violated: P2 mitigate risk");
    await expect(page.locator("#alert-body")).toHaveText(/T1 west gateway -> T1 east gateway; current 105m against 95m \+5m buffer/);
    await expect(page.locator("#alert-boundary")).toHaveText("operator review");
    await expect(page.locator("#promise-risk")).toHaveText("46");
    await expect(page.locator("#account-name")).toHaveText("Avery Chen");
    await expect(page.locator("#signal-count")).toHaveText("6 active");
    await expect(page.locator("#express-demand")).toHaveText("38%");
    await expect(page.locator("#paid-requests")).toHaveText("0");
    await expect(page.locator("#revenue-proxy")).toHaveText("$0");
    await expect(page.getByLabel("Run summary")).toContainText("baseline");
    await expect(page.locator("#summary-outcome")).toHaveText("standby");
    await expect(page.locator("#scorecard-grade")).toHaveText("DCR-0");
    await expect(page.locator("#score-renewal")).toHaveText("watch");
    await expect(page.getByLabel("Operator SLA")).toContainText("Claimbounded");
    await expect(page.locator("#sla-status")).toHaveText("simulated");
    await expect(page.locator("#sla-detect")).toHaveText("pending");
    await expect(page.getByLabel("Drive SLA priorities")).toContainText("DriveT1 west gateway -> T1 east gateway");
    await expect(page.locator("#drive-sla-status")).toHaveText("violated");
    await expect(page.locator("#drive-sla-target")).toHaveText("95m +5m buffer");
    await expect(page.locator("#drive-sla-current")).toHaveText("105m");
    await expect(page.locator("#drive-sla-priority")).toHaveText("P2 mitigate risk");
    await expect(page.locator("#drive-sla-violation")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#drive-sla-mitigation")).toHaveText("weather-responsive split + EV staging");
    await expect(page.getByLabel("Priority cascade")).toContainText("Primary PriorityP2 mitigate risk; T1 west gateway -> T1 east gateway");
    await expect(page.locator("#priority-cascade-status")).toHaveText("active");
    await expect(page.locator("#priority-cascade-sla")).toHaveText("simulated; detect pending; mitigate pending");
    await expect(page.locator("#priority-cascade-violation")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#priority-cascade-mitigation")).toHaveText("weather-responsive split + EV staging");
    await expect(page.locator("#priority-cascade-gate")).toHaveText("verify 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#priority-cascade-sponsor")).toHaveText("held: clear proof gaps before sponsor ask");
    await expect(page.locator("#priority-cascade-boundary")).toHaveText("priority cascade only; operator and owner approval still required");
    await expect(page.getByLabel("Mitigation runbook")).toContainText("Controlweather-responsive split + EV staging");
    await expect(page.locator("#mitigation-runbook-status")).toHaveText("blocked");
    await expect(page.locator("#mitigation-runbook-why")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#mitigation-runbook-gate")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#mitigation-runbook-effect")).toHaveText("+0 risk; +0% flow");
    await expect(page.locator("#mitigation-runbook-sponsor")).toHaveText("held: clear proof gaps before sponsor ask");
    await expect(page.locator("#mitigation-runbook-boundary")).toHaveText("runbook is advisory; no field command or public claim");
    await expect(page.getByLabel("Evidence queue")).toContainText("Next Owner511 closure feed");
    await expect(page.locator("#evidence-queue-status")).toHaveText("blocked");
    await expect(page.locator("#evidence-queue-readiness")).toHaveText("0/3");
    await expect(page.locator("#evidence-queue-missing")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-queue-release")).toHaveText("hold release; 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-queue-next")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.locator("#evidence-queue-boundary")).toHaveText("evidence queue only; source owners remain authoritative");
    await expect(page.getByLabel("Authority handoff")).toContainText("Current Owner511 closure feed");
    await expect(page.locator("#authority-handoff-status")).toHaveText("source-held");
    await expect(page.locator("#authority-handoff-auth")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.locator("#authority-handoff-claim")).toHaveText("3 source holds");
    await expect(page.locator("#authority-handoff-action")).toHaveText("monitor only; clear evidence first");
    await expect(page.locator("#authority-handoff-clock")).toHaveText("next monitoring cycle");
    await expect(page.locator("#authority-handoff-sponsor")).toHaveText("hold sponsor packet");
    await expect(page.locator("#authority-handoff-boundary")).toHaveText("authority handoff only; ROUTE does not approve field action");
    await expect(page.getByLabel("Recovery verification")).toContainText("Proof Windownext 2 monitoring cycles");
    await expect(page.locator("#recovery-verification-status")).toHaveText("blocked");
    await expect(page.locator("#recovery-verification-result")).toHaveText("+0 risk; +0% flow");
    await expect(page.locator("#recovery-verification-reopen")).toHaveText("watch for rebound");
    await expect(page.locator("#recovery-verification-closure")).toHaveText("clear 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#recovery-verification-proof")).toHaveText("3 source holds");
    await expect(page.locator("#recovery-verification-boundary")).toHaveText("recovery verification only; owner decides closure or renewal");
    await expect(page.getByLabel("Renewal decision gate")).toContainText("Decisionclear source custody before sponsor decision");
    await expect(page.locator("#renewal-gate-status")).toHaveText("held");
    await expect(page.locator("#renewal-gate-proof")).toHaveText("3 source holds");
    await expect(page.locator("#renewal-gate-blocker")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.locator("#renewal-gate-owner")).toHaveText("511 closure feed");
    await expect(page.locator("#renewal-gate-ask")).toHaveText("continue proof run");
    await expect(page.locator("#renewal-gate-boundary")).toHaveText("renewal decision gate only; no ROI, SLA, or renewal commitment");
    await expect(page.getByLabel("Source clearance plan")).toContainText("Next Source511 closure feed");
    await expect(page.locator("#source-clearance-status")).toHaveText("blocked");
    await expect(page.locator("#source-clearance-sequence")).toHaveText("1. 511 closure feed -> 2. Charging source owner -> 3. Operator message owner");
    await expect(page.locator("#source-clearance-readiness")).toHaveText("0/3");
    await expect(page.locator("#source-clearance-release")).toHaveText("release held until 511 closure feed");
    await expect(page.locator("#source-clearance-renewal")).toHaveText("renewal decision held");
    await expect(page.locator("#source-clearance-action")).toHaveText("verify 511 closure feed");
    await expect(page.locator("#source-clearance-boundary")).toHaveText("source clearance plan only; source owners remain authoritative");
    await expect(page.getByLabel("Clearance forecast")).toContainText("Clear Next511 closure feed");
    await expect(page.locator("#clearance-forecast-status")).toHaveText("partial");
    await expect(page.locator("#clearance-forecast-readiness")).toHaveText("1/3");
    await expect(page.locator("#clearance-forecast-remaining")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#clearance-forecast-release")).toHaveText("still held; next Charging source owner");
    await expect(page.locator("#clearance-forecast-renewal")).toHaveText("renewal still held");
    await expect(page.locator("#clearance-forecast-authority")).toHaveText("Charging source owner");
    await expect(page.locator("#clearance-forecast-boundary")).toHaveText("clearance forecast only; source owner verification still required");
    await expect(page.getByLabel("US tier map validation")).toContainText("SLA RowSAC -> SLC");
    await expect(page.locator("#tier-map-status")).toHaveText("breach");
    await expect(page.locator("#tier-map-source")).toHaveText("maps/all-tiers-v2.png + data/beck-stop-sla.csv");
    await expect(page.locator("#tier-map-path")).toHaveText("T1 via I-5;I-15");
    await expect(page.locator("#tier-map-window")).toHaveText("24h overnight");
    await expect(page.locator("#tier-map-delta")).toHaveText("+10m");
    await expect(page.locator("#tier-map-validation")).toHaveText("tier SLA breach simulated");
    await expect(page.locator("#tier-map-boundary")).toHaveText("US tier map fixture only; no live network validation");
    await expect(page.locator("#tier-route-overlay")).toContainText("SAC -> SLC; T1 via I-5;I-15");
    await expect(page.locator("#tier-route-overlay")).toContainText("SAC;CENTRAL_VALLEY;LA;BARSTOW;LAS_VEGAS");
    await expect(page.locator("#tier-origin-label")).toHaveText("SAC");
    await expect(page.locator("#tier-dest-label")).toHaveText("SLC");
    await expect(page.locator("#tier-incident-label")).toHaveText("closure risk");
    await expect(page.locator("#tier-map-segment")).toHaveText("SAC -> CENTRAL_VALLEY");
    await expect(page.locator("#tier-map-progress")).toHaveText(/^(0|8)%$/);
    await expect(page.locator("#tier-map-pressure")).toHaveText("watch +10m");
    await expect(page.locator("#tier-map-control")).toHaveText("weather-responsive split + EV staging");
    await expect(page.getByLabel("Tier decision trace")).toContainText("Why Nowwatch +10m; promise risk 46>=45");
    await expect(page.locator("#tier-trace-status")).toHaveText("held");
    await expect(page.locator("#tier-trace-evidence")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#tier-trace-release")).toHaveText("held for 511 closure feed");
    await expect(page.locator("#tier-trace-boundary")).toHaveText("decision trace only; owner approves field action");
    await expect(page.getByLabel("Tier release package")).toContainText("OwnerDOT operations");
    await expect(page.locator("#tier-release-status")).toHaveText("blocked");
    await expect(page.locator("#tier-release-criteria")).toHaveText("clear 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#tier-release-step")).toHaveText("prepare weather-responsive split + EV staging");
    await expect(page.locator("#tier-release-proof")).toContainText("SAC -> CENTRAL_VALLEY; SAC -> SLC");
    await expect(page.locator("#tier-release-boundary")).toHaveText("release package only; owner-issued field action required");
    await expect(page.getByLabel("Field preview packet")).toContainText("Signage Draftdraft only: SAC -> CENTRAL_VALLEY; watch +10m");
    await expect(page.locator("#field-preview-status")).toHaveText("held");
    await expect(page.locator("#field-preview-route")).toHaveText("prepare draft reroute split; no public detour");
    await expect(page.locator("#field-preview-ev")).toHaveText("stage EV support after source check");
    await expect(page.locator("#field-preview-audience")).toHaveText("operator review only");
    await expect(page.locator("#field-preview-boundary")).toHaveText("preview only; no public signage or routing command");
    await expect(page.getByLabel("Release readiness checklist")).toContainText("Source Custodyblocked: 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#release-check-status")).toHaveText("blocked");
    await expect(page.locator("#release-check-owner")).toHaveText("blocked until source clear");
    await expect(page.locator("#release-check-rollback")).toHaveText("watch for rebound");
    await expect(page.locator("#release-check-claims")).toHaveText("traffic control; legal detour; EV availability held");
    await expect(page.locator("#release-check-boundary")).toHaveText("readiness checklist only; no field or public release");
    await expect(page.getByLabel("Release proof ledger")).toContainText("Ledger IDDCR-LEDGER-WINTER-CLOSURE-SAC-CENTRAL-VALLEY");
    await expect(page.locator("#release-ledger-status")).toHaveText("held");
    await expect(page.locator("#release-ledger-evidence")).toHaveText("0/3; 3 source holds");
    await expect(page.locator("#release-ledger-proof")).toHaveText("blocked: 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#release-ledger-renewal")).toHaveText("hold renewal proof");
    await expect(page.locator("#release-ledger-boundary")).toHaveText("proof ledger only; not a public claim");
    await expect(page.getByLabel("Renewal proof summary")).toContainText("Sponsor Proofproof held until source custody clears");
    await expect(page.locator("#renewal-proof-status")).toHaveText("held");
    await expect(page.locator("#renewal-proof-safe")).toHaveText("readiness gaps identified");
    await expect(page.locator("#renewal-proof-held")).toHaveText("ROI; guaranteed SLA; traffic control; EV availability");
    await expect(page.locator("#renewal-proof-ask")).toHaveText("continue proof run");
    await expect(page.locator("#renewal-proof-boundary")).toHaveText("renewal summary only; no ROI or SLA guarantee");
    await expect(page.getByLabel("Sponsor packet draft")).toContainText("Askcontinue proof run");
    await expect(page.locator("#sponsor-packet-status")).toHaveText("held");
    await expect(page.locator("#sponsor-packet-proof")).toHaveText("readiness gaps identified");
    await expect(page.locator("#sponsor-packet-owner")).toHaveText("DOT operations");
    await expect(page.locator("#sponsor-packet-next")).toHaveText("clear proof gaps before sponsor ask");
    await expect(page.locator("#sponsor-packet-boundary")).toHaveText("sponsor packet draft only; no renewal commitment");
    await expect(page.getByLabel("Guidance board")).toContainText("Routedraft reroute split");
    await expect(page.locator("#guidance-status")).toHaveText("draft");
    await expect(page.locator("#guidance-source")).toHaveText(/source-needed|511 closure feed/);
    await expect(page.getByLabel("Workload board")).toContainText("Open Actions0");
    await expect(page.locator("#workload-status")).toHaveText("held");
    await expect(page.locator("#workload-source")).toHaveText("3");
    await expect(page.locator("#workload-next")).toHaveText("verify 511 closure feed");
    await expect(page.getByLabel("Operator decision log")).toContainText("Recommendednone");
    await expect(page.locator("#operator-log-status")).toHaveText("watch");
    await expect(page.locator("#operator-log-next")).toHaveText("continue monitoring");
    await expect(page.locator("#operator-log-reason")).toHaveText("no open operator action");
    await expect(page.getByLabel("Maintenance backlog")).toContainText("TicketDCR-MAINT-WINTER-CLOSURE-P2");
    await expect(page.locator("#backlog-status")).toHaveText("blocked");
    await expect(page.locator("#backlog-asset")).toHaveText("mountain corridor + charging sources");
    await expect(page.locator("#backlog-dependency")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Owner approval packet")).toContainText("OwnerDOT operations");
    await expect(page.locator("#approval-status")).toHaveText("blocked");
    await expect(page.locator("#approval-evidence")).toHaveText("0/3; 3 source holds");
    await expect(page.locator("#approval-decision")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Renewal value case")).toContainText("Askcontinue proof run");
    await expect(page.locator("#renewal-value-status")).toHaveText("work");
    await expect(page.locator("#renewal-value-gap")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#renewal-value-boundary")).toHaveText("renewal value case only; no ROI or revenue guarantee");
    await expect(page.getByLabel("System health")).toContainText("Signals6 active");
    await expect(page.locator("#system-health-status")).toHaveText("degraded");
    await expect(page.locator("#system-health-holds")).toHaveText("3");
    await expect(page.locator("#system-health-monitor")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.getByLabel("Operations cadence")).toContainText("Cycle0m");
    await expect(page.locator("#cadence-status")).toHaveText("attention");
    await expect(page.locator("#cadence-owner")).toHaveText("511 closure feed");
    await expect(page.locator("#cadence-rhythm")).toHaveText("clear 3 source holds");
    await expect(page.getByLabel("Maintenance case")).toContainText("Measured Failurecorridor reliability failure");
    await expect(page.locator("#maintenance-fix")).toHaveText("weather-responsive split + EV staging");
    await expect(page.locator("#maintenance-priority")).toHaveText("P2");
    await expect(page.locator("#maintenance-thresholds")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#maintenance-evidence")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.getByLabel("Fix package")).toContainText("OwnerDOT operations");
    await expect(page.locator("#fix-status")).toHaveText("blocked");
    await expect(page.locator("#fix-scope")).toHaveText("weather-responsive split + EV staging");
    await expect(page.locator("#fix-approval")).toHaveText("source custody");
    await expect(page.getByLabel("Fix justification")).toContainText("FailureP2 corridor reliability failure");
    await expect(page.locator("#justification-status")).toHaveText("blocked");
    await expect(page.locator("#justification-basis")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#justification-gate")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Intervention rank")).toContainText("Rankrank 2; P2");
    await expect(page.locator("#intervention-status")).toHaveText("held");
    await expect(page.locator("#intervention-confidence")).toHaveText("low");
    await expect(page.locator("#intervention-gate")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Decision dossier")).toContainText("DecisionP2 weather-responsive split + EV staging");
    await expect(page.locator("#dossier-status")).toHaveText("blocked");
    await expect(page.locator("#dossier-blockers")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Post-fix monitor")).toContainText("Windownext 2 monitoring cycles");
    await expect(page.locator("#postfix-status")).toHaveText("blocked");
    await expect(page.locator("#postfix-result")).toHaveText("+0 risk; +0% flow");
    await expect(page.getByLabel("Watchlist")).toContainText("Primary Watchsource custody");
    await expect(page.locator("#watchlist-status")).toHaveText("blocked");
    await expect(page.locator("#watchlist-owner")).toHaveText("511 closure feed");
    await expect(page.locator("#watchlist-trigger")).toHaveText("verify 511 closure feed");
    await expect(page.getByLabel("Board packet")).toContainText("Askreview P2 fix package");
    await expect(page.locator("#board-status")).toHaveText("blocked");
    await expect(page.locator("#board-sponsor")).toHaveText("DOT operations");
    await expect(page.locator("#board-gate")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Work order draft")).toContainText("Draft IDDCR-WINTER-CLOSURE-P2");
    await expect(page.locator("#workorder-status")).toHaveText("blocked");
    await expect(page.locator("#workorder-gate")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.locator("#workorder-step")).toHaveText("prepare weather-responsive split + EV staging");
    await expect(page.getByLabel("Resiliency playbook")).toContainText("SwitchMonitor only");
    await expect(page.locator("#playbook-status")).toHaveText("blocked");
    await expect(page.locator("#playbook-trigger")).toHaveText("promise risk 46>=45");
    await expect(page.locator("#playbook-gate")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Evidence dossier")).toContainText("Readiness0/3");
    await expect(page.locator("#evidence-dossier-status")).toHaveText("blocked");
    await expect(page.locator("#evidence-dossier-missing")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-dossier-claim")).toHaveText("3 source holds");
    await expect(page.getByLabel("Source escalation")).toContainText("Owner511 closure feed");
    await expect(page.locator("#source-escalation-status")).toHaveText("blocked");
    await expect(page.locator("#source-escalation-due")).toHaveText("next monitoring cycle");
    await expect(page.locator("#source-escalation-packet")).toHaveText("source custody: verify 511 closure feed");
    await expect(page.getByLabel("Closure criteria")).toContainText("Close Whenclear 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#closure-status")).toHaveText("open");
    await expect(page.locator("#closure-renew")).toHaveText("hold renewal");
    await expect(page.locator("#closure-evidence")).toHaveText("0/3");
    await expect(page.getByLabel("Operator action plan")).toContainText("Do Nowverify 511 closure feed");
    await expect(page.locator("#operator-action-status")).toHaveText("source-action");
    await expect(page.locator("#operator-action-scope")).toHaveText("source verification with 511 closure feed");
    await expect(page.locator("#operator-action-prohibited")).toHaveText("field release; public claims; renewal decision");
    await expect(page.locator("#operator-action-rerun")).toHaveText("after 511 closure feed clears");
    await expect(page.locator("#operator-action-decision")).toHaveText("watch");
    await expect(page.locator("#operator-action-cadence")).toHaveText("clear 3 source holds");
    await expect(page.locator("#operator-action-boundary")).toHaveText("operator action plan only; no field command authority");
    await expect(page.getByLabel("Next-cycle simulation")).toContainText("Clear Assumption511 closure feed");
    await expect(page.locator("#next-cycle-status")).toHaveText("still-held");
    await expect(page.locator("#next-cycle-readiness")).toHaveText("1/3");
    await expect(page.locator("#next-cycle-remaining")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#next-cycle-sla")).toHaveText("SLA violated; weather-responsive split + EV staging");
    await expect(page.locator("#next-cycle-move")).toHaveText("verify Charging source owner");
    await expect(page.locator("#next-cycle-release")).toHaveText("still held for Charging source owner");
    await expect(page.locator("#next-cycle-boundary")).toHaveText("next-cycle simulation only; source owners and field owners remain authoritative");
    await expect(page.getByLabel("Clearance path")).toContainText("Cycles Left3");
    await expect(page.locator("#clearance-path-status")).toHaveText("queued");
    await expect(page.locator("#clearance-path-sequence")).toHaveText("cycle 1: 511 closure feed -> cycle 2: Charging source owner -> cycle 3: Operator message owner");
    await expect(page.locator("#clearance-path-readiness")).toHaveText("3/3");
    await expect(page.locator("#clearance-path-release")).toHaveText("after Operator message owner clears");
    await expect(page.locator("#clearance-path-sponsor")).toHaveText("sponsor packet held until source custody clears");
    await expect(page.locator("#clearance-path-proof")).toHaveText("0/3 now; 3 source cycles to clear");
    await expect(page.locator("#clearance-path-boundary")).toHaveText("clearance path is simulated sequencing only; source owners approve evidence and field owners approve release");
    await expect(page.getByLabel("Release criteria")).toContainText("Source Gate0/3 ready; missing 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#release-criteria-status")).toHaveText("source-held");
    await expect(page.locator("#release-criteria-sla")).toHaveText("violated; 105m against 95m +5m buffer");
    await expect(page.locator("#release-criteria-owner")).toHaveText("not before Operator message owner clears");
    await expect(page.locator("#release-criteria-claims")).toHaveText("traffic control; legal detour; EV availability; guaranteed SLA held");
    await expect(page.locator("#release-criteria-decision")).toHaveText("clear 511 closure feed");
    await expect(page.locator("#release-criteria-evidence")).toHaveText("source custody: 511 closure feed");
    await expect(page.locator("#release-criteria-boundary")).toHaveText("release criteria only; owner approval and field release remain external");
    await expect(page.getByLabel("Decision audit")).toContainText("Triggerclear 511 closure feed");
    await expect(page.locator("#decision-audit-status")).toHaveText("held");
    await expect(page.locator("#decision-audit-rationale")).toHaveText("P2 corridor reliability failure; risk 46; flow 91%; recovery 42m");
    await expect(page.locator("#decision-audit-evidence")).toHaveText("source custody: 511 closure feed; 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#decision-audit-recommendation")).toHaveText("clear 511 closure feed");
    await expect(page.locator("#decision-audit-blocker")).toHaveText("0/3 ready; missing 511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#decision-audit-id")).toHaveText("DCR-AUDIT-WINTER-CLOSURE-SOURCE-HELD");
    await expect(page.locator("#decision-audit-boundary")).toHaveText("decision audit only; recommendations require owner approval before field release");
    await expect(page.getByLabel("Signal confidence")).toContainText("Confidencelow confidence");
    await expect(page.locator("#signal-confidence-status")).toHaveText("evidence-held");
    await expect(page.locator("#signal-confidence-proof")).toHaveText("0/3 evidence; DCR-0; degraded");
    await expect(page.locator("#signal-confidence-action")).toHaveText("clear 511 closure feed");
    await expect(page.locator("#signal-confidence-gap")).toHaveText("511 closure feed; Charging source owner; Operator message owner");
    await expect(page.locator("#signal-confidence-refresh")).toHaveText("clear 3 source holds");
    await expect(page.locator("#signal-confidence-escalation")).toHaveText("511 closure feed");
    await expect(page.locator("#signal-confidence-boundary")).toHaveText("signal confidence is advisory; source evidence and owner approval remain authoritative");
    await expect(page.getByLabel("Portfolio proof")).toContainText("Saved Runs0");
    await expect(page.locator("#portfolio-grade")).toHaveText("0 runs");
    await expect(page.getByLabel("Renewal backlog")).toContainText("No saved renewal work");
    await expect(page.locator("#renewal-backlog-count")).toHaveText("0 items");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/ROUTE DCR shift handoff - 00:(00|05)/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Next action: verify 511 closure feed/);
    await expect(page.getByLabel("Scenario run plan")).toContainText("Primary pass restriction crosses promise-risk threshold.");
    await expect(page.getByLabel("Evidence gates")).toContainText("511 closure feed");
    await expect(page.locator("#gate-count")).toHaveText("0/3 ready");
    await expect(page.getByLabel("Timeline queue")).toContainText("00:10 Snow band reduces alternate reliability");
    await expect(page.getByLabel("Action queue")).toContainText("No open action");
    await expect(page.locator("#run-owner")).toHaveText("DOT operations");
    await expect(page.getByText("Held traffic control, legal detour, SLA, EV availability, pricing authority")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/held_claims/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sla_boundary,"simulated timing; no guaranteed SLA"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/drive_sla_status,"violated"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/drive_sla_current,"105m"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/priority_cascade_status,"active"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/priority_cascade_sponsor_step,"held: clear proof gaps before sponsor ask"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/mitigation_runbook_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/mitigation_runbook_gate,"511 closure feed; Charging source owner; Operator message owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_queue_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_queue_next_action,"source custody: verify 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/authority_handoff_status,"source-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/authority_handoff_owner,"511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/recovery_verification_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/recovery_verification_sponsor_proof,"3 source holds"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_gate_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_gate_open_blocker,"source custody: verify 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_clearance_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_clearance_action,"verify 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_forecast_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_forecast_next_authority,"Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_action_status,"source-action"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_action_do_now,"verify 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/next_cycle_status,"still-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/next_cycle_operator_move,"verify Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_path_status,"queued"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_path_cycles_left,"3"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_criteria_status,"source-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_criteria_decision,"clear 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/decision_audit_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/decision_audit_trigger,"clear 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/signal_confidence_status,"evidence-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/signal_confidence_actionability,"clear 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_map_status,"breach"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_map_source,"maps\/all-tiers-v2.png \+ data\/beck-stop-sla.csv"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_map_boundary,"US tier map fixture only; no live network validation"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_sim_segment,"SAC -> CENTRAL_VALLEY"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_sim_pressure,"watch \+10m"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_trace_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_trace_release,"held for 511 closure feed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_release_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_release_criteria,"clear 511 closure feed; Charging source owner; Operator message owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/field_preview_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/field_preview_boundary,"preview only; no public signage or routing command"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_check_status,"blocked"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_check_owner,"blocked until source clear"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_ledger_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_ledger_id,"DCR-LEDGER-WINTER-CLOSURE-SAC-CENTRAL-VALLEY"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_proof_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_proof_ask,"continue proof run"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sponsor_packet_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sponsor_packet_next_step,"clear proof gaps before sponsor ask"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/guidance_boundary,"advisory, not field command"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/workload_queue,"source custody"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_boundary,"recommendation only; owner approval required"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_thresholds,"promise risk 46>=45"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/fix_boundary,"recommendation package, not a work order"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/justification_boundary,"justification only; owner approves fixes"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/intervention_boundary,"ranked recommendation only; no dispatch authority"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/dossier_boundary,"advisory dossier, not procurement approval"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/postfix_boundary,"monitoring only; owner decides field changes"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/watchlist_boundary,"watchlist only; no dispatch"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/board_boundary,"board packet only; sponsor decision required"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/workorder_boundary,"draft only; owner-issued work order required"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/playbook_boundary,"playbook is advisory; operator and owner authority required"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_dossier_boundary,"evidence dossier only; source owners remain authoritative"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_escalation_boundary,"escalation only; source owner remains authoritative"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/closure_boundary,"closure criteria only; owner closes or renews"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_log_boundary,"operator decision log only; field authority remains external"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_backlog_boundary,"backlog ticket only; owner CMMS remains source of record"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/owner_approval_boundary,"approval packet only; owner approval is external"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_value_boundary,"renewal value case only; no ROI or revenue guarantee"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/system_health_boundary,"system health is simulated; no field-device status claim"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/cadence_boundary,"cadence is simulated; operator owns live operations"/);
  });

  test("controls create and approve switch packets without changing authority boundary", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByRole("button", { name: "Reroute" }).click();
    await expect(page.getByText("Reroute advisory created from simulated signal review.")).toBeVisible();
    await expect(page.locator("#decision-count")).toHaveText("0 reviewed");
    await expect(page.locator("#operator-log-status")).toHaveText("review");
    await expect(page.locator("#operator-log-next")).toHaveText("review Reroute advisory");
    await expect(page.locator("#operator-log-reason")).toHaveText("Reroute advisory");

    await page.getByLabel("Active role").selectOption("operator");
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.locator("#decision-count")).toHaveText("1 reviewed");
    await expect(page.getByText("Operator reviewed advisory. ROUTE still does not command field devices, set prices, or publish claims.")).toBeVisible();
    await expect(page.locator("#operator-log-status")).toHaveText("approved");
    await expect(page.locator("#operator-log-approved")).toHaveText("Reroute advisory");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_status,"approved"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/active_role,"Operator"/);
  });

  test("scenario and injected signals update cockpit state and readout export", async ({ page }) => {
    await page.setViewportSize({ width: 390, height: 840 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByLabel("Scenario case").selectOption("terminal-access");
    await expect(page.locator("#active-case")).toHaveText("Terminal access disruption");
    await expect(page.locator("#promise-risk")).toHaveText("52");
    await expect(page.locator("#run-owner")).toHaveText("Port operations");
    await expect(page.locator("#source-status")).toHaveText("source-needed");
    await expect(page.getByLabel("Scenario run plan")).toContainText("Terminal queue owner plus local truck-route authority required.");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_owner,"Port operations"/);
    await expect(page.locator("#guidance-route")).toHaveText("draft reroute split");
    await expect(page.locator("#guidance-signage")).toHaveText("queue early signage");
    await expect(page.locator("#maintenance-failure")).toHaveText("intersection access failure");
    await expect(page.locator("#maintenance-fix")).toHaveText("retime gate approach routing");
    await expect(page.locator("#maintenance-priority")).toHaveText("P1");
    await expect(page.locator("#maintenance-thresholds")).toHaveText("promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#drive-sla-endpoints")).toHaveText("Inland yard -> Port gate");
    await expect(page.locator("#drive-sla-current")).toHaveText("69m");
    await expect(page.locator("#drive-sla-priority")).toHaveText("P1 protect drive SLA");
    await expect(page.locator("#drive-sla-violation")).toHaveText("promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#tier-map-row")).toHaveText("HOU -> BEAUMONT");
    await expect(page.locator("#tier-map-path")).toHaveText("T1 via I-10");
    await expect(page.locator("#tier-map-window")).toHaveText("6h local");
    await expect(page.locator("#tier-map-delta")).toHaveText("+27m");
    await expect(page.locator("#tier-route-overlay")).toContainText("HOU -> BEAUMONT; T1 via I-10; HOU;BEAUMONT");
    await expect(page.locator("#tier-origin-label")).toHaveText("HOU");
    await expect(page.locator("#tier-dest-label")).toHaveText("BEAUMONT");
    await expect(page.locator("#tier-incident-label")).toHaveText("gate queue");
    await expect(page.locator("#tier-map-segment")).toHaveText("HOU -> BEAUMONT");
    await expect(page.locator("#tier-map-pressure")).toHaveText("critical +27m");
    await expect(page.locator("#tier-map-control")).toHaveText("retime gate approach routing");
    await expect(page.locator("#tier-trace-status")).toHaveText("held");
    await expect(page.locator("#tier-trace-release")).toHaveText("held for Terminal queue feed");
    await expect(page.locator("#tier-trace-move")).toHaveText("retime gate approach routing");
    await expect(page.locator("#fix-owner")).toHaveText("Port operations");
    await expect(page.locator("#fix-scope")).toHaveText("retime gate approach routing");
    await expect(page.locator("#justification-failure")).toHaveText("P1 intersection access failure");
    await expect(page.locator("#justification-basis")).toHaveText("promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#justification-gate")).toHaveText("source custody: verify Terminal queue feed");
    await expect(page.locator("#intervention-rank")).toHaveText("rank 1; P1");
    await expect(page.locator("#intervention-name")).toHaveText("retime gate approach routing");
    await expect(page.locator("#intervention-value")).toHaveText("promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#dossier-alternatives")).toHaveText("hold gate timing; manual detour only");
    await expect(page.locator("#postfix-target")).toHaveText("risk below baseline; flow above baseline");
    await expect(page.locator("#watchlist-cycle")).toHaveText("next 2 monitoring cycles");
    await expect(page.locator("#board-case")).toHaveText("intersection access failure; retime gate approach routing");
    await expect(page.locator("#workorder-id")).toHaveText("DCR-TERMINAL-ACCESS-P1");
    await expect(page.locator("#workorder-step")).toHaveText("prepare retime gate approach routing");
    await expect(page.locator("#playbook-fallback")).toHaveText("hold gate timing; manual detour only");
    await expect(page.locator("#playbook-gate")).toHaveText("source custody: verify Terminal queue feed");
    await expect(page.locator("#evidence-dossier-basis")).toHaveText("promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#source-escalation-owner")).toHaveText("Terminal queue feed");
    await expect(page.locator("#source-escalation-reason")).toHaveText("P1 intersection access failure; promise risk 52>=45; flow 84%<85%");
    await expect(page.locator("#closure-close")).toHaveText("clear Terminal queue feed; Local truck route authority; Port communications owner");
    await expect(page.locator("#backlog-ticket")).toHaveText("DCR-MAINT-TERMINAL-ACCESS-P1");
    await expect(page.locator("#backlog-asset")).toHaveText("terminal gate approach");
    await expect(page.locator("#approval-owner")).toHaveText("Port operations");
    await expect(page.locator("#approval-request")).toHaveText("DCR-TERMINAL-ACCESS-P1: prepare retime gate approach routing");
    await expect(page.locator("#renewal-value-coverage")).toHaveText("terminal gate approach");
    await expect(page.locator("#system-health-holds")).toHaveText("3");
    await expect(page.locator("#cadence-owner")).toHaveText("Terminal queue feed");
    await expect(page.locator("#cadence-rhythm")).toHaveText("clear 3 source holds");

    await page.getByRole("button", { name: "Terminal", exact: true }).click();
    await expect(page.locator("#promise-risk")).toHaveText("57");
    await expect(page.locator("#network-flow")).toHaveText("80%");
    await expect(page.getByLabel("Timeline queue")).toContainText("manual");
    await expect(page.getByLabel("Action queue")).toContainText("Reroute advisory");
    await page.getByRole("button", { name: "EV Support" }).click();
    await page.getByRole("button", { name: "Hold For Authority" }).click();
    await expect(page.getByText("Held for authority. Traffic control, legal detour, SLA, EV availability, pricing authority, and revenue guarantees remain blocked claims.")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/manual_events,"Terminal access exception added"/);

    const downloadPromise = page.waitForEvent("download");
    await page.getByRole("button", { name: "Download CSV" }).click();
    const download = await downloadPromise;
    expect(download.suggestedFilename()).toBe("route-dcr-cockpit-readout.csv");
  });

  test("scheduled timeline events evolve risk and flow deterministically", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await expect(page.locator("#promise-risk")).toHaveText("46");
    await expect(page.locator("#network-flow")).toHaveText("91%");
    await expect(page.locator("#timeline-count")).toHaveText("0 applied");

    await page.getByRole("button", { name: "Step", exact: true }).click();
    await expect(page.locator("#promise-risk")).toHaveText("46");
    await expect(page.locator("#timeline-count")).toHaveText("0 applied");

    await page.getByRole("button", { name: "Step", exact: true }).click();
    await expect(page.locator("#promise-risk")).toHaveText("51");
    await expect(page.locator("#network-flow")).toHaveText("89%");
    await expect(page.locator("#risk-delta")).toHaveText("+5");
    await expect(page.locator("#timeline-count")).toHaveText("1 applied");
    await expect(page.locator("#tier-map-segment")).toHaveText("CENTRAL_VALLEY -> LA");
    await expect(page.locator("#tier-map-progress")).toHaveText("16%");
    await expect(page.getByLabel("Timeline queue")).toContainText("applied");
    await expect(page.locator("#action-count")).toHaveText("1 open");
    await expect(page.getByLabel("Action queue")).toContainText("Reroute advisory");
    await expect(page.locator("#guidance-status")).toHaveText("held");
    await expect(page.locator("#guidance-route")).toHaveText("draft reroute split");
    await expect(page.locator("#guidance-source")).toHaveText("511 closure feed");
    await expect(page.locator("#workload-open")).toHaveText("1");
    await expect(page.locator("#workload-owner")).toHaveText("Operator");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/timeline_events,"Snow band reduces alternate reliability"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_sim_segment,"CENTRAL_VALLEY -> LA"/);

    await page.getByRole("button", { name: "Create Reroute advisory" }).click();
    await page.getByLabel("Active role").selectOption("operator");
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.getByText("Held for source custody: 511 closure feed.")).toBeVisible();
    await expect(page.locator("#action-count")).toHaveText("1 open");
    await expect(page.locator("#summary-outcome")).toHaveText("source-held");
    await expect(page.locator("#summary-open-work")).toHaveText("1");
    await expect(page.locator("#sla-status")).toHaveText("held");
    await expect(page.locator("#sla-hold")).toHaveText("0m");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/missing_evidence,"511 closure feed; Charging source owner; Operator message owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/run_outcome,"source-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sla_status,"held"/);

    await page.getByRole("button", { name: "Verify 511 closure feed" }).click();
    await expect(page.locator("#gate-count")).toHaveText("1/3 ready");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_ready,"1\/3"/);
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.locator("#promise-risk")).toHaveText("42");
    await expect(page.locator("#network-flow")).toHaveText("96%");
    await expect(page.locator("#action-count")).toHaveText("0 open");
    await expect(page.locator("#summary-outcome")).toHaveText("mitigated");
    await expect(page.locator("#summary-risk-change")).toHaveText("-4");
    await expect(page.locator("#summary-flow-change")).toHaveText("+5%");
    await expect(page.locator("#summary-evidence-ready")).toHaveText("1/3");
    await expect(page.locator("#summary-pilot-value")).toHaveText("proof-ready");
    await expect(page.locator("#scorecard-grade")).toHaveText("DCR-4");
    await expect(page.locator("#score-response")).toHaveText("0m");
    await expect(page.locator("#score-mitigation")).toHaveText("100%");
    await expect(page.locator("#score-evidence")).toHaveText("33%");
    await expect(page.locator("#score-renewal")).toHaveText("renew");
    await expect(page.locator("#sla-status")).toHaveText("mitigated");
    await expect(page.locator("#sla-verify")).toHaveText("0m");
    await expect(page.locator("#sla-mitigate")).toHaveText("0m");
    await expect(page.locator("#drive-sla-status")).toHaveText("maintained");
    await expect(page.locator("#drive-sla-current")).toHaveText("97m");
    await expect(page.locator("#drive-sla-priority")).toHaveText("P3 maintain watch");
    await expect(page.locator("#drive-sla-violation")).toHaveText("none");
    await expect(page.locator("#drive-sla-gate")).toHaveText("verify Charging source owner; Operator message owner");
    await expect(page.locator("#priority-cascade-status")).toHaveText("maintained");
    await expect(page.locator("#priority-cascade-primary")).toHaveText("P3 maintain watch; T1 west gateway -> T1 east gateway");
    await expect(page.locator("#priority-cascade-sla")).toHaveText("mitigated; detect 0m; mitigate 0m");
    await expect(page.locator("#priority-cascade-violation")).toHaveText("none");
    await expect(page.locator("#priority-cascade-mitigation")).toHaveText("monitor rebound and preserve proof");
    await expect(page.locator("#priority-cascade-sponsor")).toHaveText("decide renewal candidate");
    await expect(page.locator("#mitigation-runbook-status")).toHaveText("proving");
    await expect(page.locator("#mitigation-runbook-control")).toHaveText("Reroute advisory");
    await expect(page.locator("#mitigation-runbook-why")).toHaveText("SLA maintained; preserve proof window");
    await expect(page.locator("#mitigation-runbook-gate")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#mitigation-runbook-effect")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#mitigation-runbook-sponsor")).toHaveText("decide renewal candidate");
    await expect(page.locator("#evidence-queue-status")).toHaveText("partial");
    await expect(page.locator("#evidence-queue-owner")).toHaveText("Charging source owner");
    await expect(page.locator("#evidence-queue-readiness")).toHaveText("1/3");
    await expect(page.locator("#evidence-queue-missing")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-queue-release")).toHaveText("hold release; Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-queue-next")).toHaveText("source custody: verify Charging source owner");
    await expect(page.locator("#authority-handoff-status")).toHaveText("source-held");
    await expect(page.locator("#authority-handoff-owner")).toHaveText("Charging source owner");
    await expect(page.locator("#authority-handoff-auth")).toHaveText("source custody: verify Charging source owner");
    await expect(page.locator("#authority-handoff-claim")).toHaveText("2 source holds");
    await expect(page.locator("#authority-handoff-clock")).toHaveText("next monitoring cycle");
    await expect(page.locator("#authority-handoff-sponsor")).toHaveText("hold sponsor packet");
    await expect(page.locator("#recovery-verification-status")).toHaveText("proving");
    await expect(page.locator("#recovery-verification-window")).toHaveText("next 2 monitoring cycles");
    await expect(page.locator("#recovery-verification-target")).toHaveText("risk below baseline; flow above baseline");
    await expect(page.locator("#recovery-verification-result")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#recovery-verification-closure")).toHaveText("clear Charging source owner; Operator message owner");
    await expect(page.locator("#recovery-verification-proof")).toHaveText("monitored DCR proof retained");
    await expect(page.locator("#renewal-gate-status")).toHaveText("held");
    await expect(page.locator("#renewal-gate-decision")).toHaveText("clear source custody before sponsor decision");
    await expect(page.locator("#renewal-gate-proof")).toHaveText("monitored DCR proof retained");
    await expect(page.locator("#renewal-gate-blocker")).toHaveText("source custody: verify Charging source owner");
    await expect(page.locator("#renewal-gate-owner")).toHaveText("Charging source owner");
    await expect(page.locator("#renewal-gate-ask")).toHaveText("continue proof run");
    await expect(page.locator("#source-clearance-status")).toHaveText("partial");
    await expect(page.locator("#source-clearance-next")).toHaveText("Charging source owner");
    await expect(page.locator("#source-clearance-sequence")).toHaveText("1. Charging source owner -> 2. Operator message owner");
    await expect(page.locator("#source-clearance-readiness")).toHaveText("1/3");
    await expect(page.locator("#source-clearance-release")).toHaveText("release held until Charging source owner");
    await expect(page.locator("#source-clearance-renewal")).toHaveText("renewal decision held");
    await expect(page.locator("#source-clearance-action")).toHaveText("verify Charging source owner");
    await expect(page.locator("#clearance-forecast-status")).toHaveText("partial");
    await expect(page.locator("#clearance-forecast-next")).toHaveText("Charging source owner");
    await expect(page.locator("#clearance-forecast-readiness")).toHaveText("2/3");
    await expect(page.locator("#clearance-forecast-remaining")).toHaveText("Operator message owner");
    await expect(page.locator("#clearance-forecast-release")).toHaveText("still held; next Operator message owner");
    await expect(page.locator("#clearance-forecast-renewal")).toHaveText("renewal still held");
    await expect(page.locator("#clearance-forecast-authority")).toHaveText("Operator message owner");
    await expect(page.locator("#recommended-posture")).toHaveText("weather-responsive split + EV staging; continue monitoring");
    await expect(page.locator("#alert-title")).toHaveText("maintained: P3 maintain watch");
    await expect(page.locator("#alert-boundary")).toHaveText("monitor");
    await expect(page.locator("#guidance-status")).toHaveText("reviewed");
    await expect(page.locator("#guidance-route")).toHaveText("reviewed alternate route");
    await expect(page.locator("#guidance-source")).toHaveText("operator-reviewed");
    await expect(page.locator("#workload-status")).toHaveText("held");
    await expect(page.locator("#workload-open")).toHaveText("0");
    await expect(page.locator("#workload-source")).toHaveText("2");
    await expect(page.locator("#workload-next")).toHaveText("verify Charging source owner");
    await expect(page.locator("#operator-log-status")).toHaveText("approved");
    await expect(page.locator("#operator-log-approved")).toHaveText("Reroute advisory");
    await expect(page.locator("#operator-log-next")).toHaveText("continue monitoring");
    await expect(page.locator("#backlog-status")).toHaveText("renewal");
    await expect(page.locator("#backlog-queue")).toHaveText("renewal candidate");
    await expect(page.locator("#backlog-exit")).toHaveText("clear Charging source owner; Operator message owner");
    await expect(page.locator("#approval-status")).toHaveText("review");
    await expect(page.locator("#approval-evidence")).toHaveText("1/3; 2 source holds");
    await expect(page.locator("#approval-decision")).toHaveText("decide renewal candidate");
    await expect(page.locator("#renewal-value-status")).toHaveText("renew");
    await expect(page.locator("#renewal-value-ask")).toHaveText("renew monitored DCR cockpit");
    await expect(page.locator("#renewal-value-next")).toHaveText("renewal candidate");
    await expect(page.locator("#system-health-status")).toHaveText("degraded");
    await expect(page.locator("#system-health-holds")).toHaveText("2");
    await expect(page.locator("#system-health-controls")).toHaveText("paused");
    await expect(page.locator("#cadence-status")).toHaveText("attention");
    await expect(page.locator("#cadence-owner")).toHaveText("Charging source owner");
    await expect(page.locator("#cadence-escalation")).toHaveText("reopen if target misses");
    await expect(page.locator("#cadence-rhythm")).toHaveText("clear 2 source holds");
    await expect(page.locator("#maintenance-status")).toHaveText("validated");
    await expect(page.locator("#maintenance-thresholds")).toHaveText("none crossed");
    await expect(page.locator("#maintenance-evidence")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#evidence-dossier-status")).toHaveText("partial");
    await expect(page.locator("#evidence-dossier-readiness")).toHaveText("1/3");
    await expect(page.locator("#evidence-dossier-ready")).toHaveText("511 closure feed");
    await expect(page.locator("#evidence-dossier-claim")).toHaveText("2 source holds");
    await expect(page.locator("#source-escalation-status")).toHaveText("partial");
    await expect(page.locator("#source-escalation-owner")).toHaveText("Charging source owner");
    await expect(page.locator("#source-escalation-proof")).toHaveText("Charging source owner");
    await expect(page.locator("#closure-status")).toHaveText("proving");
    await expect(page.locator("#closure-close")).toHaveText("clear Charging source owner; Operator message owner");
    await expect(page.locator("#closure-renew")).toHaveText("package renewal candidate");
    await expect(page.locator("#fix-status")).toHaveText("validated");
    await expect(page.locator("#fix-effect")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#fix-approval")).toHaveText("source custody");
    await expect(page.locator("#justification-status")).toHaveText("validated");
    await expect(page.locator("#justification-expected")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#justification-proof")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#justification-gate")).toHaveText("source custody: verify Charging source owner");
    await expect(page.locator("#intervention-status")).toHaveText("proving");
    await expect(page.locator("#intervention-rank")).toHaveText("rank 3; P3");
    await expect(page.locator("#intervention-confidence")).toHaveText("medium");
    await expect(page.locator("#intervention-value")).toHaveText("DCR-4; renew");
    await expect(page.locator("#intervention-gate")).toHaveText("decide renewal candidate");
    await expect(page.locator("#dossier-status")).toHaveText("validated");
    await expect(page.locator("#dossier-why")).toHaveText("risk 42; flow 96%; recovery 43m");
    await expect(page.locator("#dossier-proof")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#postfix-status")).toHaveText("proving");
    await expect(page.locator("#postfix-result")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#postfix-rollback")).toHaveText("watch for rebound");
    await expect(page.locator("#watchlist-status")).toHaveText("active");
    await expect(page.locator("#watchlist-primary")).toHaveText("rebound watch");
    await expect(page.locator("#watchlist-escalation")).toHaveText("reopen if target misses");
    await expect(page.locator("#board-status")).toHaveText("ready");
    await expect(page.locator("#board-value")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#board-gate")).toHaveText("source custody: verify Charging source owner");
    await expect(page.locator("#workorder-status")).toHaveText("ready");
    await expect(page.locator("#workorder-gate")).toHaveText("sponsor decision + owner issue");
    await expect(page.locator("#workorder-proof")).toHaveText("-4 risk; +5% flow");
    await expect(page.locator("#playbook-status")).toHaveText("active");
    await expect(page.locator("#playbook-switch")).toHaveText("Reroute advisory");
    await expect(page.locator("#playbook-trigger")).toHaveText("post-fix proving window");
    await expect(page.locator("#playbook-gate")).toHaveText("owner-issued action; monitor rebound");
    await expect(page.locator("#handoff-status")).toHaveText("mitigated");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Outcome: mitigated; pilot value: proof-ready/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/SLA view: detect 0m, hold 0m, verify 0m, mitigate 0m/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Drive SLA: maintained; T1 west gateway -> T1 east gateway; current 97m against 95m \+5m buffer; priority P3 maintain watch; violation none; mitigation weather-responsive split \+ EV staging/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Priority cascade: maintained; primary P3 maintain watch; T1 west gateway -> T1 east gateway; SLA mitigated; detect 0m; mitigate 0m; violation none; mitigation monitor rebound and preserve proof; gate verify Charging source owner; Operator message owner; sponsor decide renewal candidate/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Mitigation runbook: proving; control Reroute advisory; why SLA maintained; preserve proof window; gate Charging source owner; Operator message owner; effect -4 risk; \+5% flow; rollback watch for rebound; sponsor decide renewal candidate/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Evidence queue: partial; owner Charging source owner; readiness 1\/3; missing Charging source owner; Operator message owner; claim 2 source holds; release hold release; Charging source owner; Operator message owner; next source custody: verify Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Authority handoff: source-held; owner Charging source owner; authorization source custody: verify Charging source owner; claim 2 source holds; action monitor only; clear evidence first; clock next monitoring cycle; sponsor hold sponsor packet/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Recovery verification: proving; window next 2 monitoring cycles; target risk below baseline; flow above baseline; result -4 risk; \+5% flow; reopen watch for rebound; closure clear Charging source owner; Operator message owner; proof monitored DCR proof retained/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Renewal decision gate: held; decision clear source custody before sponsor decision; proof monitored DCR proof retained; blocker source custody: verify Charging source owner; owner Charging source owner; ask continue proof run; held ROI; guaranteed SLA; traffic control; EV availability/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Source clearance plan: partial; next Charging source owner; sequence 1\. Charging source owner -> 2\. Operator message owner; readiness 1\/3; release release held until Charging source owner; renewal renewal decision held; action verify Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Clearance forecast: partial; clear Charging source owner; after readiness 2\/3; remaining Operator message owner; release still held; next Operator message owner; renewal renewal still held; authority Operator message owner/);
    await expect(page.locator("#operator-action-status")).toHaveText("source-action");
    await expect(page.locator("#operator-action-now")).toHaveText("verify Charging source owner");
    await expect(page.locator("#operator-action-scope")).toHaveText("source verification with Charging source owner");
    await expect(page.locator("#operator-action-prohibited")).toHaveText("field release; public claims; renewal decision");
    await expect(page.locator("#operator-action-rerun")).toHaveText("after Charging source owner clears");
    await expect(page.locator("#operator-action-decision")).toHaveText("approved");
    await expect(page.locator("#operator-action-cadence")).toHaveText("clear 2 source holds");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Operator action plan: source-action; now verify Charging source owner; scope source verification with Charging source owner; prohibited field release; public claims; renewal decision; rerun after Charging source owner clears; decision approved; cadence clear 2 source holds/);
    await expect(page.locator("#next-cycle-status")).toHaveText("still-held");
    await expect(page.locator("#next-cycle-clear")).toHaveText("Charging source owner");
    await expect(page.locator("#next-cycle-readiness")).toHaveText("2/3");
    await expect(page.locator("#next-cycle-remaining")).toHaveText("Operator message owner");
    await expect(page.locator("#next-cycle-sla")).toHaveText("SLA maintained; monitor rebound and preserve proof");
    await expect(page.locator("#next-cycle-move")).toHaveText("verify Operator message owner");
    await expect(page.locator("#next-cycle-release")).toHaveText("still held for Operator message owner");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Next-cycle simulation: still-held; clear Charging source owner; readiness 2\/3; remaining Operator message owner; posture SLA maintained; monitor rebound and preserve proof; move verify Operator message owner; release still held for Operator message owner/);
    await expect(page.locator("#clearance-path-status")).toHaveText("queued");
    await expect(page.locator("#clearance-path-cycles")).toHaveText("2");
    await expect(page.locator("#clearance-path-sequence")).toHaveText("cycle 1: Charging source owner -> cycle 2: Operator message owner");
    await expect(page.locator("#clearance-path-readiness")).toHaveText("3/3");
    await expect(page.locator("#clearance-path-release")).toHaveText("after Operator message owner clears");
    await expect(page.locator("#clearance-path-sponsor")).toHaveText("sponsor packet held until source custody clears");
    await expect(page.locator("#clearance-path-proof")).toHaveText("1/3 now; 2 source cycles to clear");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Clearance path: queued; cycles 2; sequence cycle 1: Charging source owner -> cycle 2: Operator message owner; final readiness 3\/3; release after Operator message owner clears; sponsor sponsor packet held until source custody clears; proof 1\/3 now; 2 source cycles to clear/);
    await expect(page.locator("#release-criteria-status")).toHaveText("source-held");
    await expect(page.locator("#release-criteria-source")).toHaveText("1/3 ready; missing Charging source owner; Operator message owner");
    await expect(page.locator("#release-criteria-sla")).toHaveText("maintained; 97m against 95m +5m buffer");
    await expect(page.locator("#release-criteria-owner")).toHaveText("not before Operator message owner clears");
    await expect(page.locator("#release-criteria-decision")).toHaveText("clear Charging source owner");
    await expect(page.locator("#release-criteria-evidence")).toHaveText("source custody: Charging source owner");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Release criteria: source-held; source 1\/3 ready; missing Charging source owner; Operator message owner; SLA maintained; 97m against 95m \+5m buffer; owner not before Operator message owner clears; claims traffic control; legal detour; EV availability; guaranteed SLA held; decision clear Charging source owner; evidence source custody: Charging source owner/);
    await expect(page.locator("#decision-audit-status")).toHaveText("held");
    await expect(page.locator("#decision-audit-trigger")).toHaveText("clear Charging source owner");
    await expect(page.locator("#decision-audit-rationale")).toHaveText("P3 corridor reliability failure; risk 42; flow 96%; recovery 43m");
    await expect(page.locator("#decision-audit-evidence")).toHaveText("source custody: Charging source owner; Charging source owner; Operator message owner");
    await expect(page.locator("#decision-audit-recommendation")).toHaveText("clear Charging source owner");
    await expect(page.locator("#decision-audit-blocker")).toHaveText("1/3 ready; missing Charging source owner; Operator message owner");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Decision audit: held; trigger clear Charging source owner; rationale P3 corridor reliability failure; risk 42; flow 96%; recovery 43m; evidence source custody: Charging source owner; Charging source owner; Operator message owner; recommendation clear Charging source owner; blocked by 1\/3 ready; missing Charging source owner; Operator message owner; id DCR-AUDIT-WINTER-CLOSURE-SOURCE-HELD/);
    await expect(page.locator("#signal-confidence-status")).toHaveText("evidence-held");
    await expect(page.locator("#signal-confidence-grade")).toHaveText("low confidence");
    await expect(page.locator("#signal-confidence-proof")).toHaveText("1/3 evidence; DCR-4; degraded");
    await expect(page.locator("#signal-confidence-action")).toHaveText("clear Charging source owner");
    await expect(page.locator("#signal-confidence-gap")).toHaveText("Charging source owner; Operator message owner");
    await expect(page.locator("#signal-confidence-refresh")).toHaveText("clear 2 source holds");
    await expect(page.locator("#signal-confidence-escalation")).toHaveText("Charging source owner");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Signal confidence: evidence-held; grade low confidence; proof 1\/3 evidence; DCR-4; degraded; action clear Charging source owner; gap Charging source owner; Operator message owner; refresh clear 2 source holds; escalation Charging source owner/);
    await expect(page.locator("#tier-map-status")).toHaveText("validated");
    await expect(page.locator("#tier-map-delta")).toHaveText("+2m");
    await expect(page.locator("#tier-map-validation")).toHaveText("validated within tier buffer");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/US tier map validation: validated; row SAC -> SLC; path T1 via I-5;I-15; window 24h overnight; delta \+2m; validated within tier buffer/);
    await expect(page.locator("#tier-trace-status")).toHaveText("monitor");
    await expect(page.locator("#tier-trace-release")).toHaveText("monitor release");
    await expect(page.locator("#tier-trace-move")).toHaveText("continue monitoring");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Tier decision trace: monitor; why buffer holding at \+2m; none; evidence Charging source owner; Operator message owner; release monitor release; move continue monitoring/);
    await expect(page.locator("#tier-release-status")).toHaveText("monitor");
    await expect(page.locator("#tier-release-criteria")).toHaveText("maintain Drive SLA through next 2 monitoring cycles");
    await expect(page.locator("#tier-release-step")).toHaveText("keep advisory in watch mode");
    await expect(page.locator("#tier-release-proof")).toHaveText("post-fix proving window");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Tier release package: monitor; owner DOT operations; criteria maintain Drive SLA through next 2 monitoring cycles; step keep advisory in watch mode; rollback watch for rebound; proof post-fix proving window/);
    await expect(page.locator("#field-preview-status")).toHaveText("watch");
    await expect(page.locator("#field-preview-signage")).toHaveText("watch CENTRAL_VALLEY -> LA; SLA buffer holding");
    await expect(page.locator("#field-preview-route")).toHaveText("reviewed alternate route");
    await expect(page.locator("#field-preview-audience")).toHaveText("operator watch desk");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Field preview packet: watch; signage watch CENTRAL_VALLEY -> LA; SLA buffer holding; route reviewed alternate route; EV monitor range; hold Charging source owner; Operator message owner/);
    await expect(page.locator("#release-check-status")).toHaveText("watch");
    await expect(page.locator("#release-check-source")).toHaveText("blocked: Charging source owner; Operator message owner");
    await expect(page.locator("#release-check-owner")).toHaveText("DOT operations monitor only");
    await expect(page.locator("#release-check-proof")).toHaveText("post-fix proving window");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Release readiness checklist: watch; source blocked: Charging source owner; Operator message owner; owner DOT operations monitor only; rollback watch for rebound; claims traffic control; legal detour; EV availability held; proof post-fix proving window/);
    await expect(page.locator("#release-ledger-status")).toHaveText("retained");
    await expect(page.locator("#release-ledger-id")).toHaveText("DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA");
    await expect(page.locator("#release-ledger-evidence")).toHaveText("1/3; 2 source holds");
    await expect(page.locator("#release-ledger-proof")).toHaveText("post-fix proving window");
    await expect(page.locator("#release-ledger-renewal")).toHaveText("renew monitored DCR cockpit");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Release proof ledger: retained; id DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA; evidence 1\/3; 2 source holds; proof post-fix proving window; renewal renew monitored DCR cockpit; review DOT operations; DOT operations monitor only/);
    await expect(page.locator("#renewal-proof-status")).toHaveText("ready");
    await expect(page.locator("#renewal-proof-sponsor")).toHaveText("DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA; DCR-4; -4 risk; +5% flow; 1 resolved");
    await expect(page.locator("#renewal-proof-safe")).toHaveText("monitored DCR proof retained");
    await expect(page.locator("#renewal-proof-ask")).toHaveText("renew monitored DCR cockpit");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Renewal proof summary: ready; sponsor proof DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA; DCR-4; -4 risk; \+5% flow; 1 resolved; safe claim monitored DCR proof retained; ask renew monitored DCR cockpit; held ROI; guaranteed SLA; traffic control; EV availability/);
    await expect(page.locator("#sponsor-packet-status")).toHaveText("ready");
    await expect(page.locator("#sponsor-packet-ask")).toHaveText("renew monitored DCR cockpit");
    await expect(page.locator("#sponsor-packet-proof")).toHaveText("DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA; DCR-4; -4 risk; +5% flow; 1 resolved");
    await expect(page.locator("#sponsor-packet-next")).toHaveText("decide renewal candidate");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Sponsor packet draft: ready; ask renew monitored DCR cockpit; proof DCR-LEDGER-WINTER-CLOSURE-CENTRAL-VALLEY-LA; DCR-4; -4 risk; \+5% flow; 1 resolved; owner DOT operations; held ROI; guaranteed SLA; traffic control; EV availability; next decide renewal candidate/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Guidance: reviewed; route reviewed alternate route/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Workload: held; queue source custody; next owner Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Maintenance: P3 corridor reliability failure; fix weather-responsive split \+ EV staging; thresholds none crossed; evidence gap Charging source owner; Operator message owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Fix package: validated; owner DOT operations; scope weather-responsive split \+ EV staging; effect -4 risk; \+5% flow; approval source custody/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Fix justification: validated; failure P3 corridor reliability failure; basis risk 42; flow 96%; recovery 43m; expected -4 risk; \+5% flow; gate source custody: verify Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Intervention rank: proving; rank 3; P3; intervention weather-responsive split \+ EV staging; confidence medium; gate decide renewal candidate/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Dossier: decision P3 weather-responsive split \+ EV staging; why risk 42; flow 96%; recovery 43m; blockers source custody: verify Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Post-fix monitor: proving; target risk below baseline; flow above baseline; result -4 risk; \+5% flow; rollback watch for rebound/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Watchlist: active; primary rebound watch; owner Charging source owner; trigger watch for rebound; escalation reopen if target misses/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Board packet: ready; ask review P3 fix package; sponsor DOT operations; value -4 risk; \+5% flow; gate source custody: verify Charging source owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Work order draft: ready; id DCR-WINTER-CLOSURE-P3; issuer DOT operations; gate sponsor decision \+ owner issue; step issue weather-responsive split \+ EV staging/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Resiliency playbook: active; switch Reroute advisory; trigger post-fix proving window; rollback watch for rebound; gate owner-issued action; monitor rebound/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Evidence dossier: partial; readiness 1\/3; missing Charging source owner; Operator message owner; basis risk 42; flow 96%; recovery 43m; claim 2 source holds/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Source escalation: partial; owner Charging source owner; due next monitoring cycle; packet source custody: verify Charging source owner; boundary escalation only; source owner remains authoritative/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Closure criteria: proving; close clear Charging source owner; Operator message owner; renew package renewal candidate; reopen watch for rebound; evidence 1\/3/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Operator decision log: approved; recommended none; approved Reroute advisory; held none; next continue monitoring/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Maintenance backlog: renewal; ticket DCR-MAINT-WINTER-CLOSURE-P3; queue renewal candidate; dependency source custody: verify Charging source owner; exit clear Charging source owner; Operator message owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Owner approval packet: review; owner DOT operations; request DCR-WINTER-CLOSURE-P3: issue weather-responsive split \+ EV staging; evidence 1\/3; 2 source holds; decision decide renewal candidate/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Renewal value case: renew; ask renew monitored DCR cockpit; proof DCR-4; -4 risk; \+5% flow; 1 resolved; coverage mountain corridor \+ charging sources; gap Charging source owner; Operator message owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/System health: degraded; signals 6 active; source holds 2; controls paused; monitor Charging source owner; Operator message owner/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Operations cadence: attention; cycle 0m; checkpoint next 2 monitoring cycles; owner Charging source owner; escalation reopen if target misses/);
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Next action: verify Charging source owner/);
    await expect(page.getByLabel("Timeline queue")).toContainText("resolved");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/resolved_events,"1"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/pilot_value,"proof-ready"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/scorecard_grade,"DCR-4"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/time_to_mitigate,"0m"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/drive_sla_status,"maintained"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/drive_sla_violation,"none"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/priority_cascade_status,"maintained"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/priority_cascade_mitigation,"monitor rebound and preserve proof"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/mitigation_runbook_status,"proving"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/mitigation_runbook_control,"Reroute advisory"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_queue_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_queue_owner,"Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/authority_handoff_status,"source-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/authority_handoff_authorization,"source custody: verify Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/recovery_verification_status,"proving"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/recovery_verification_result,"-4 risk; \+5% flow"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_gate_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_gate_owner,"Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_clearance_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_clearance_next_source,"Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_forecast_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_forecast_after_readiness,"2\/3"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_action_status,"source-action"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_action_do_now,"verify Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/next_cycle_status,"still-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/next_cycle_clear_assumption,"Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_path_cycles_left,"2"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/clearance_path_proof_trail,"1\/3 now; 2 source cycles to clear"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_criteria_status,"source-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_criteria_sla_gate,"maintained; 97m against 95m \+5m buffer"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/decision_audit_status,"held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/decision_audit_recommendation,"clear Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/signal_confidence_status,"evidence-held"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/signal_confidence_proof_quality,"1\/3 evidence; DCR-4; degraded"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_map_status,"validated"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_map_validation,"validated within tier buffer"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_trace_status,"monitor"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_trace_release,"monitor release"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_release_status,"monitor"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/tier_release_step,"keep advisory in watch mode"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/field_preview_status,"watch"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/field_preview_route,"reviewed alternate route"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_check_status,"watch"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_check_proof,"post-fix proving window"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_ledger_status,"retained"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/release_ledger_renewal,"renew monitored DCR cockpit"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_proof_status,"ready"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_proof_safe_claim,"monitored DCR proof retained"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sponsor_packet_status,"ready"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/sponsor_packet_next_step,"decide renewal candidate"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/guidance_status,"reviewed"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/workload_source_holds,"2"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_status,"validated"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_fix,"weather-responsive split \+ EV staging"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_justification,"risk 42; flow 96%; recovery 43m"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_evidence_gap,"Charging source owner; Operator message owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/fix_expected_effect,"-4 risk; \+5% flow"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/justification_status,"validated"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/justification_approval_gate,"source custody: verify Charging source owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/intervention_status,"proving"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/intervention_release_gate,"decide renewal candidate"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/dossier_status,"validated"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/postfix_status,"proving"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/watchlist_status,"active"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/board_status,"ready"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/workorder_status,"ready"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/playbook_status,"active"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/evidence_dossier_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/source_escalation_status,"partial"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/closure_status,"proving"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_log_status,"approved"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/maintenance_backlog_status,"renewal"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/owner_approval_status,"review"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/renewal_value_status,"renew"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/system_health_status,"degraded"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/cadence_status,"attention"/);

    const handoffPromise = page.waitForEvent("download");
    await page.getByRole("button", { name: "Download TXT" }).click();
    const handoffDownload = await handoffPromise;
    expect(handoffDownload.suggestedFilename()).toBe("route-dcr-shift-handoff.txt");
  });

  test("demo director runs the guided DCR sequence", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByRole("button", { name: "Next Demo Step" }).click();
    await expect(page.locator("#clock")).toHaveText("00:10");
    await expect(page.locator("#director-status")).toHaveText("Applied event: Snow band reduces alternate reliability.");
    await expect(page.locator("#action-count")).toHaveText("1 open");

    await page.getByRole("button", { name: "Next Demo Step" }).click();
    await expect(page.getByText("Reroute advisory created from simulated signal review.")).toBeVisible();
    await expect(page.locator("#director-status")).toHaveText("Created switch packet: Reroute advisory.");

    await page.getByRole("button", { name: "Next Demo Step" }).click();
    await expect(page.getByText("Held for source custody: 511 closure feed.")).toBeVisible();
    await expect(page.locator("#summary-outcome")).toHaveText("source-held");

    await page.getByRole("button", { name: "Next Demo Step" }).click();
    await expect(page.locator("#gate-count")).toHaveText("1/3 ready");
    await expect(page.locator("#director-status")).toHaveText("Verified source gate: 511 closure feed.");

    await page.getByRole("button", { name: "Next Demo Step" }).click();
    await expect(page.locator("#summary-outcome")).toHaveText("mitigated");
    await expect(page.locator("#action-count")).toHaveText("0 open");
    await expect(page.locator("#scorecard-grade")).toHaveText("DCR-4");
    await expect(page.locator("#sla-status")).toHaveText("mitigated");
    await expect(page.locator("#guidance-status")).toHaveText("reviewed");
    await expect(page.locator("#workload-next")).toHaveText("verify Charging source owner");
    await expect(page.locator("#maintenance-status")).toHaveText("validated");
    await expect(page.getByLabel("Shift handoff")).toHaveValue(/Outcome: mitigated; pilot value: proof-ready/);

    await page.getByRole("button", { name: "Save Run" }).click();
    await expect(page.locator("#portfolio-grade")).toHaveText("proof");
    await expect(page.locator("#portfolio-runs")).toHaveText("1");
    await expect(page.locator("#portfolio-proof")).toHaveText("1");
    await expect(page.locator("#portfolio-mitigated")).toHaveText("1");
    await expect(page.locator("#portfolio-renewal")).toHaveText("1");
    await expect(page.locator("#renewal-backlog-count")).toHaveText("2 items");
    await expect(page.getByLabel("Renewal backlog")).toContainText("Renew proven DCR coverage");
    await expect(page.getByLabel("Renewal backlog")).toContainText("Expand scenario coverage");
  });

  test("express payment signal creates a bounded service advisory", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByRole("button", { name: "Payment" }).click();
    await expect(page.getByText("Express service payment advisory created from simulated signal review.")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/pending_or_held_switches,"Express service payment advisory: pending"/);

    await page.getByRole("button", { name: "Priority advisory $45" }).click();
    await expect(page.locator("#paid-requests")).toHaveText("10");
    await expect(page.locator("#revenue-proxy")).toHaveText("$450");
    await expect(page.getByText("Priority advisory is simulated at $45.")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/pricing_status,"simulated only; owner authority required"/);

    await page.getByLabel("Active role").selectOption("payment");
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.locator("#decision-count")).toHaveText("1 reviewed");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/approved_switches,"Express service payment advisory"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/pricing_authority; revenue_guarantee/);

    await page.getByRole("button", { name: "Verified window $120" }).click();
    await expect(page.locator("#paid-requests")).toHaveText("5");
    await expect(page.locator("#revenue-proxy")).toHaveText("$600");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/express_tier,"Verified window"/);
  });

  test("role permissions block mismatched approvals", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByRole("button", { name: "Payment" }).click();
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.getByText("Held for Planner authority mismatch. Planner can create advisory packets only.")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/operator_status,"held"/);

    await page.getByLabel("Active role").selectOption("payment");
    await page.getByRole("button", { name: "Approve Reviewed Switch" }).click();
    await expect(page.getByText("Payment owner reviewed advisory. ROUTE still does not command field devices, set prices, or publish claims.")).toBeVisible();
    await expect(page.getByLabel("Executive readout")).toHaveValue(/active_role,"Payment owner"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/role_authority,"can approve express payment posture"/);
  });

  test("local accounts save and replay cockpit runs", async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 820 });
    await page.goto(pagePath);
    await page.getByRole("button", { name: "Pause" }).click();

    await page.getByLabel("Account profile").selectOption("operator");
    await expect(page.locator("#account-name")).toHaveText("Marta Ruiz");
    await expect(page.getByLabel("Active role")).toHaveValue("operator");
    await expect(page.getByText("Marta Ruiz at District Operations. Local demo account; no authentication backend.")).toBeVisible();

    await page.getByLabel("Scenario case").selectOption("managed-lane");
    await page.getByRole("button", { name: "Step", exact: true }).click();
    await page.getByRole("button", { name: "Step", exact: true }).click();
    await expect(page.getByLabel("Timeline queue")).toContainText("Clearance estimate slips by one cycle");
    await page.getByRole("button", { name: "Save Run" }).click();
    await expect(page.locator("#saved-run-status")).toContainText("Saved Managed-lane incident recovery for Marta Ruiz");
    await expect(page.getByLabel("Run library")).toContainText("Managed-lane incident recovery - Marta Ruiz @ 00:10");

    await page.getByLabel("Account profile").selectOption("planner");
    await page.getByLabel("Scenario case").selectOption("winter-closure");
    await expect(page.locator("#account-name")).toHaveText("Avery Chen");

    await page.getByRole("button", { name: "Load Last" }).click();
    await expect(page.locator("#account-name")).toHaveText("Marta Ruiz");
    await expect(page.locator("#active-case")).toHaveText("Managed-lane incident recovery");
    await expect(page.locator("#clock")).toHaveText("00:10");
    await expect(page.getByLabel("Executive readout")).toHaveValue(/account_name,"Marta Ruiz"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/account_org,"District Operations"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/risk_delta,"7"/);
    await expect(page.getByLabel("Executive readout")).toHaveValue(/timeline_events,"Clearance estimate slips by one cycle"/);
    await expect(page.locator("#risk-delta")).toHaveText("+7");

    await page.getByLabel("Scenario case").selectOption("freight-bottleneck");
    await page.getByRole("button", { name: "Replay Selected" }).click();
    await expect(page.locator("#active-case")).toHaveText("Managed-lane incident recovery");

    const exportPromise = page.waitForEvent("download");
    await page.getByRole("button", { name: "Export Runs" }).click();
    const exportDownload = await exportPromise;
    expect(exportDownload.suggestedFilename()).toBe("route-dcr-cockpit-runs.json");

    await page.getByRole("button", { name: "Clear Runs" }).click();
    await expect(page.locator("#saved-run-status")).toHaveText("Saved runs cleared.");
    await expect(page.locator("#portfolio-runs")).toHaveText("0");
    await expect(page.locator("#renewal-backlog-count")).toHaveText("0 items");
  });
});
