# Design yield

This directory is intentionally empty of promoted corridor designs.

ROUTE’s pipeline can produce concepts and holds; a file lands here only after
parliament/editorial review clears a design for this tree. Until then, empty is
the honest state — not a missing checkout.

See `BACKLOG.md` item Y1 and `README.md` lab posture.
